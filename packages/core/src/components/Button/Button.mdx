import {  Story, Canvas } from '@storybook/addon-docs/blocks';
import * as stories from './Button.stories.tsx';
import Button  from './Button';


# Button

I can embed a story (but not define one, since this file should not contain a `Meta`):
<Canvas>
  <Story id="component-button-primary" name={"Primary"} story={stories.Primary} >
    <Button foo={'foo test'} onClick={() => alert("click mdx")} />
  </Story>
</Canvas>

And I can also embed arbitrary markdown & JSX in this file.

<Button foo={'foo test'} onClick={() => alert("click mdx")} />
