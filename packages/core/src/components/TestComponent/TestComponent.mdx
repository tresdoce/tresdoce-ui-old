import { Meta, Preview } from '@storybook/addon-docs/blocks';
import { createTheme } from '@tresdoce-ui/brand';
import Layout  from '../Layout';

import TestComponent from './TestComponent';

<Meta title="Component/TestComponent" component={TestComponent} />

# TestComponent

I can embed a story (but not define one, since this file should not contain a `Meta`):

<Preview >
    <Layout theme={createTheme()} cdnBasepath={''} containerFluid={true}>
      <TestComponent type={'primary'}/>
    </Layout>
</Preview>


And I can also embed arbitrary markdown & JSX in this file.

<Preview >
  <Layout theme={createTheme()} cdnBasepath={''} >
    <TestComponent type={'secondary'}/>
  </Layout>
</Preview>

```javascript
theme: 'primary' | 'secondary'
```
